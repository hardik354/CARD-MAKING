<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body> 
    <div class=" w-full h-full bg-zinc-900 p-10 text-white">

        <div class="nav mb-10">
            <a class="text-red-800" href="/read">Read users</a>
        </div>

        <h1 class="text-3xl tracking-tighter mb-3 ">UPDATE USER</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-2 m-1 border-2 border-zinc-200 rounded-lg outline-none bg-transparent" type="text" name="name" placeholder="Name" value="<%= user.name %>">
            <input class="px-5 py-2  m-1 border-2 border-zinc-200 rounded-lg outline-none bg-transparent" type="text" name="email" placeholder="Email" value="<%= user.email %>" >
            <input class="px-5 py-2 m-1 border-2 border-zinc-200 rounded-lg outline-none bg-transparent" type="text" name="image" placeholder="Image url" value="<%= user.image %>" >
            <input class="px-5 py-2 ml-3 bg-yellow-300 text-black rounded-lg" type ="submit" value="Update User">
        </form>
    </div>
</body>
</html>
