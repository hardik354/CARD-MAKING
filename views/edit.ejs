<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Adding background animation */
        body {
            background: url('https://via.placeholder.com/1920x1080') no-repeat center center fixed;
            background-size: cover;
            animation: bgFade 15s infinite alternate;
        }

        @keyframes bgFade {
            0% { filter: brightness(0.8); }
            50% { filter: brightness(1); }
            100% { filter: brightness(0.8); }
        }
    </style>
</head>
<body>
    <div class="min-h-screen flex items-center justify-center p-5 bg-black/50">
        <div class="w-full max-w-md bg-white/80 backdrop-blur-md rounded-lg p-6 shadow-2xl transform hover:scale-105 transition-transform duration-300">
            <!-- Navigation -->
            <div class="nav mb-10 text-center">
                <a class="text-red-800 hover:text-red-600 font-bold text-xl transition duration-300" href="/read">Read Users</a>
            </div>

            <!-- Heading -->
            <h1 class="text-4xl font-extrabold text-center tracking-tight mb-6 text-yellow-400 animate-pulse">Update User</h1>

            <!-- Form -->
            <form action="/update/<%= user._id %>" method="post" class="space-y-4">
                <input class="w-full px-4 py-3 bg-white border border-gray-300 rounded-lg outline-none focus:border-yellow-400 transition duration-300 shadow-sm" type="text" name="name" placeholder="Name" value="<%= user.name %>">
                
                <input class="w-full px-4 py-3 bg-white border border-gray-300 rounded-lg outline-none focus:border-yellow-400 transition duration-300 shadow-sm" type="text" name="email" placeholder="Email" value="<%= user.email %>">
                
                <input class="w-full px-4 py-3 bg-white border border-gray-300 rounded-lg outline-none focus:border-yellow-400 transition duration-300 shadow-sm" type="text" name="image" placeholder="Image URL" value="<%= user.image %>">
                
                <button class="w-full py-3 bg-gradient-to-r from-yellow-400 to-orange-500 hover:from-orange-500 hover:to-yellow-400 text-black font-semibold rounded-lg transition duration-300 transform hover:scale-105" type="submit">Update User</button>
            </form>
        </div>
    </div>
</body>
</html>
